# ArtisanHub — мультивендорный маркетплейс товаров ручной работы

> Этот документ — твоё техническое задание и roadmap. Я буду выступать куратором:
> когда дойдёшь до очередного этапа, присылай код — разберём вместе, найдём баги,
> обсудим архитектурные решения. Не торопись проходить все этапы сразу — лучше
> сделать 3 этапа хорошо, чем 10 наспех.

## Зачем именно эта идея

Твой предыдущий проект (блог с постами и одной подпиской) был *линейным*:
один автор → один пост → одна подписка → один платёж. Здесь всё иначе —
появляется **множественность и согласованность состояний**:

- Один заказ может содержать товары от **нескольких продавцов одновременно**
  → нужно разбивать заказ на под-заказы и синхронизировать их статусы.
- Деньги должны **расщепляться между продавцами и платформой** (комиссия) —
  это уже не "оплатил и всё", а маркетплейс-платёж (Stripe Connect).
- Остатки товара меняются **параллельно** (гонки при одновременной покупке
  последней единицы) — потребуется думать о конкурентности на уровне БД.
- Нужен **real-time** слой (чат покупатель↔продавец, уведомления) — впервые
  выходишь за пределы обычного REST request/response.
- Поиск с фильтрами и фасетами (цена, категория, рейтинг, наличие) —
  первый раз столкнёшься с оптимизацией запросов на масштабе.

Это тот же набор технологий (Django REST + Vue 3/Pinia), но с гораздо более
сложной предметной областью — то, с чем ты реально столкнёшься на работе.

---

## 1. Предметная область

Платформа, где независимые продавцы ("мастерские") выставляют товары
(handmade, digital-товары, услуги), а покупатели формируют один заказ
из товаров разных продавцов, оплачивают его одним платежом, который
затем автоматически распределяется между продавцами за вычетом комиссии
платформы.

### Роли пользователей
- **Buyer** — покупатель: просмотр, избранное, корзина, заказы, отзывы, чат.
- **Seller** — продавец: управляет магазином (Shop), товарами, остатками,
  обрабатывает под-заказы, отвечает в чате, видит аналитику продаж.
- **Admin** — модерация отзывов и магазинов, разбор споров (disputes),
  общая аналитика платформы.

Один пользователь может быть одновременно и Buyer, и Seller (как на Etsy/Ozon).

---

## 2. Функциональные модули

### 2.1 Каталог и товары (`catalog`)
- Модели: `Shop`, `Product`, `ProductVariant` (размер/цвет/материал — со своей
  ценой и остатком), `Category` (с вложенностью через `MPTT` или `parent`
  self-FK), `ProductImage` (несколько изображений на товар).
- Полнотекстовый + фасетный поиск: фильтры по цене, категории, наличию,
  рейтингу магазина, сортировка (по цене, новизне, популярности).
- Остатки (`stock`) списываются **атомарно** при оформлении заказа —
  без гонок (`select_for_update` или `F()`-выражения).

### 2.2 Избранное (`favorites`)
- Избранные товары **и** избранные магазины отдельно.
- Уведомление, если избранный товар подешевел или скоро закончится
  (Celery-задача, периодическая проверка).

### 2.3 Корзина и заказы (`orders`)
- Корзина хранит позиции из разных магазинов.
- При оформлении заказ **разбивается на под-заказы** (`SubOrder`) — по одному
  на каждый магазин, каждый со своим статусом
  (`pending → confirmed → shipped → delivered → cancelled/returned`).
- Родительский `Order` агрегирует статус (например, "частично доставлен").
- История статусов (`OrderStatusHistory`) — кто и когда сменил статус.
- Отмена заказа покупателем возможна только на определённых статусах —
  нужна явная state machine (можно вручную, можно `django-fsm`).

### 2.4 Оплата (`payments`) — самая сложная часть
- Интеграция **Stripe Connect** (Standard или Express account для продавцов):
  платформа принимает платёж целиком, затем автоматически переводит каждому
  продавцу его долю за вычетом комиссии платформы (`application_fee_amount`).
- Обработка вебхуков: `checkout.session.completed`,
  `account.updated` (статус верификации продавца),
  `charge.refunded`, `payout.paid`.
- Частичные возвраты — если покупатель возвращает товар от одного из
  нескольких продавцов в заказе, а не весь заказ целиком.
- Идемпотентность вебхуков (как в исходном проекте, но с большим числом
  типов событий и партиями выплат).

### 2.5 Подписки продавцов (`subscriptions`)
Не подписка покупателя на контент (как в прошлом проекте), а **тарифные
планы для продавцов**:
- `Basic` — до 10 товаров, комиссия 8%.
- `Pro` — до 100 товаров, комиссия 5%, приоритет в поиске.
- `Premium` — безлимит, комиссия 3%, аналитика, промо на главной.
- Ежемесячное продление через Stripe Subscriptions, downgrade с сохранением
  превышающих лимит товаров как "скрытых" (не публикуются, пока не уменьшат
  количество или не проапгрейдятся).

### 2.6 Отзывы и рейтинги (`reviews`)
- Отзыв можно оставить **только после доставленного заказа** (проверка через
  `OrderItem`).
- Модерация: отзыв уходит в очередь на проверку при жалобе / низкой оценке,
  админ может скрыть.
- Рейтинг магазина — агрегат (пересчитывается через сигнал или Celery-задачу,
  не на каждый GET).

### 2.7 Чат в реальном времени (`chat`)
- Django Channels + WebSocket: диалог покупатель↔продавец, привязанный
  к конкретному заказу или товару.
- Статусы "прочитано/не прочитано", индикатор "печатает".
- Fallback на обычный REST-эндпоинт истории сообщений (для первой загрузки).

### 2.8 Уведомления (`notifications`)
- In-app + email: новый заказ (продавцу), смена статуса (покупателю),
  новое сообщение в чате, товар из избранного скоро закончится.
- Одна модель `Notification` с `type` и `payload` (JSON), не отдельная
  таблица на каждый тип события.

### 2.9 Промокоды и скидки (`promotions`)
- Промокод может быть привязан к конкретному магазину или ко всей платформе.
- Проценты / фиксированная сумма / бесплатная доставка.
- Ограничения: даты действия, минимальная сумма заказа, лимит использований
  (в том числе на одного пользователя).

---

## 3. Технический стек (усложнение относительно прошлого проекта)

| Слой | Технология | Что нового по сравнению с прошлым проектом |
|---|---|---|
| Backend | Django REST Framework | + Django Channels (WebSocket) |
| Платежи | Stripe **Connect** | Мульти-получательские платежи, а не просто Checkout |
| Очереди | Celery + Redis | Больше задач: пересчёт рейтингов, уведомления, проверка остатков |
| Real-time | Django Channels + Redis (channel layer) | Совсем новый слой |
| Поиск | Postgres full-text search или Elasticsearch (по желанию) | Фасетный поиск с фильтрами |
| Frontend | Vue 3 + Pinia | + WebSocket-клиент, сложные формы (варианты товара) |
| Тесты | pytest-django + factory_boy | Обязательное условие (см. ниже) |
| Инфраструктура | Docker Compose | + сервис Channels/ASGI (Daphne/Uvicorn) отдельно от WSGI |

---

## 4. Модель данных (черновой набросок, не догма)

```
User (buyer / seller flag)
Shop (owner: User, subscription_plan, stripe_account_id, rating_cached)
Category (parent: self, nullable)
Product (shop, category, base_price, is_published)
ProductVariant (product, sku, attributes: JSON, price_override, stock)
ProductImage (product, image, order)

Favorite (user, product | shop)  — generic relation либо два поля nullable

Cart (user, is_active)
CartItem (cart, variant, quantity)

Order (buyer, total_amount, status, created_at)
SubOrder (order, shop, status, subtotal, commission_amount)
OrderItem (suborder, variant, quantity, price_at_purchase)
OrderStatusHistory (suborder, from_status, to_status, changed_by, created_at)

Payment (order, stripe_payment_intent_id, status, amount)
SellerPayout (suborder, shop, amount, stripe_transfer_id, status)
Refund (payment | suborder, amount, reason, status)

SubscriptionPlan (name, product_limit, commission_rate, price)
ShopSubscription (shop, plan, status, current_period_end)

Review (order_item, author, shop_or_product, rating, comment, status: pending/approved/hidden)

ChatThread (order, buyer, shop)
ChatMessage (thread, sender, content, is_read, created_at)

Notification (user, type, payload, is_read, created_at)

PromoCode (shop nullable, code, discount_type, value, valid_from, valid_to, usage_limit, used_count)
```

Подумай сам, где нужны индексы (`unique_together`, составные индексы под
частые фильтры), где `select_related`/`prefetch_related` обязателен —
не копируй модели вслепую, это твоя часть работы.

---

## 5. Roadmap — этапы (checkpoints)

Присылай код после каждого этапа — не жди, пока сделаешь всё разом.
Так проще ловить архитектурные ошибки, пока их не отросло 50 штук.

### Этап 0 — Фундамент (1 неделя)
- Настроить Django + DRF + Postgres + Docker Compose (backend + db + redis).
- Кастомная модель `User` с разделением buyer/seller (или отдельная модель
  `Shop` с owner).
- JWT-аутентификация.
- **Цель:** не переусложнять, просто рабочий скелет + `pytest` подключён
  и хотя бы один тест реально проходит в CI (или локально).

### Этап 1 — Каталог (1-2 недели)
- CRUD магазинов, категорий, товаров, вариантов, изображений.
- Публичный список товаров с фильтрами и пагинацией.
- **Проверка себя:** сделай `N+1`-запрос специально, посмотри в
  `django-debug-toolbar`, почини через `select_related`. Это упражнение
  обязательно, не пропускай.

### Этап 2 — Избранное и корзина (несколько дней)
- Избранное для товаров и магазинов.
- Корзина с товарами из разных магазинов.
- **Ловушка, в которую стоит специально попасть и выбраться:** что
  происходит с `CartItem`, если продавец удалил вариант товара, пока он
  лежал в чужой корзине? Продумай `on_delete` и валидацию при чекауте.

### Этап 3 — Заказы и разбивка на под-заказы (1-2 недели)
- Оформление заказа: корзина → `Order` + несколько `SubOrder`.
- Списание остатков **атомарно** (`transaction.atomic()` +
  `select_for_update()` или `F()`).
- State machine статусов под-заказа.
- **Обязательный тест:** напиши тест, который имитирует два одновременных
  запроса на покупку последней единицы товара (через threading или
  просто через явную проверку остатка после `select_for_update`).
  Убедись, что второй покупатель получает ошибку "нет в наличии", а не
  уходит в минус.

### Этап 4 — Платежи через Stripe Connect (2-3 недели, самый сложный этап)
- Onboarding продавца (Stripe Express account, redirect на верификацию).
- Checkout с разбивкой платежа (`application_fee_amount` на каждый
  под-заказ или один платёж + отдельные `Transfer` после подтверждения).
- Обработка вебхуков идемпотентно (как в прошлом проекте, но с бо́льшим
  числом типов событий).
- Частичный возврат по одному под-заказу.
- **Проверка себя:** пройди official Stripe Connect testing guide,
  используй тестовые аккаунты, специально сломай вебхук (отправь дважды
  одно событие) и убедись, что деньги не спишутся/начислятся дважды.

### Этап 5 — Подписки продавцов (1 неделя)
- Тарифные планы, Stripe Subscriptions, лимит товаров по плану.
- Downgrade с корректной обработкой превышения лимита.

### Этап 6 — Отзывы и модерация (несколько дней)
- Отзыв только после доставки.
- Пересчёт рейтинга через сигнал или периодическую задачу — обоснуй выбор
  в комментарии к коду.
- Простая очередь модерации для админа.

### Этап 7 — Real-time чат (1-2 недели, новый для тебя стек)
- Django Channels, WebSocket consumer, аутентификация по токену в WS.
- История сообщений через REST, live-обновления через WS.
- **Начни с малого:** сначала просто echo-сервер на Channels, чтобы
  разобраться с consumer/routing/channel layer, и только потом делай чат.

### Этап 8 — Уведомления и промокоды (несколько дней)
- Единая модель уведомлений + email через Celery.
- Промокоды с валидацией лимитов и дат.

### Этап 9 — Frontend (параллельно с бэкендом, по своему темпу)
- Vue 3 + Pinia, но в этот раз:
  - переменные окружения через `import.meta.env`, никаких хардкод-URL;
  - WebSocket-клиент с переподключением при обрыве связи;
  - форма товара с динамическими вариантами (добавление/удаление строк);
  - корзина, сгруппированная по магазинам в UI.

### Этап 10 — Тесты и линтеры (сквозной этап, не "потом")
- Минимум: happy-path тест на каждый ключевой флоу (регистрация → магазин →
  товар → заказ → оплата → отзыв).
- `ruff`/`flake8` + `eslint` в CI обязательны с первого коммита, а не
  добавляются в конце, как это было в прошлый раз.

---

## 6. Как я буду тебя курировать

На каждом этапе присылай:
1. Что реализовал (кратко, своими словами — не код сразу).
2. Код модуля.
3. Конкретный вопрос или "что вызывает сомнения".

Я буду:
- искать баги и объяснять **почему** это баг, а не просто чинить;
- указывать на N+1, гонки, отсутствие индексов, проблемы идемпотентности;
- спрашивать "а что будет, если...", чтобы ты сам продумывал edge-cases
  (что если продавец отклонил заказ после оплаты, что если товар удалили
  посреди чекаута, что если вебхук пришёл раньше, чем клиент вернулся
  на success_url).

## 7. Критерии "проект готов" (definition of done)

- Одновременная покупка последней единицы товара не даёт уйти остатку в минус.
- Повторная доставка одного и того же Stripe-вебхука не создаёт дублей
  платежей/начислений.
- Заказ с товарами от 3 разных продавцов корректно разбивается, каждый
  под-заказ независимо меняет статус.
- Чат работает в реальном времени между двумя открытыми вкладками браузера.
- Есть хотя бы 15-20 осмысленных тестов, а не заглушки `pass`.
- Ни одного хардкод-URL/креда в коде фронтенда и бэкенда.

## 8. Опциональные stretch-цели (если захочешь пойти дальше)

- Рекомендации "похожие товары" (простая content-based модель по категориям
  и тегам, без ML-хайпа).
- Полнотекстовый поиск на Elasticsearch/OpenSearch вместо Postgres FTS.
- Мультиязычность каталога (`django-modeltranslation` или свой подход).
- Разбор споров (disputes) между покупателем и продавцом с участием админа.
- Rate limiting на чувствительные эндпоинты (оформление заказа, применение
  промокода).

---

Начни с Этапа 0 и присылай код — разберём вместе.